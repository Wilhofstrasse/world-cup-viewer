# Transient review state — never commit
review/inbox/
.refine.lock

# Mac
.DS_Store

# Secrets / env
.env
.env.local
.dev.vars

# Build output
dist/
build/
node_modules/
.wrangler/

# Smoke / agent artifacts (kept in /tmp, never committed)
qa-screenshots/
qa-report.json
test-qa.mjs
review-tmp.js
wm-smoke*.mjs

# Goal-detector local state + heavyweight models
tools/goal-detector/.state.json
tools/goal-detector/.tmp/
tools/goal-detector/models/*.bin

# Visual-diff harness — test results + HTML report + lockfile.
# Baselines (visual/specs/**/*.png) are committed; outputs are not.
visual/test-results/
visual/report/
visual/.run.lock
